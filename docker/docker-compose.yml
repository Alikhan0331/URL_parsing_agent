services:
  event-agent:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    env_file:
      - ../.env
    volumes:
      - ../output:/app/output
    command: ["--base-url", "https://qr-pib.kz/ru/post/?page={n}", "--max-pages", "3"]
