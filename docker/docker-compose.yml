services:
  event-agent-scheduler:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    env_file:
      - ../.env
    volumes:
      - ../output:/app/output
      - ../config:/app/config
    restart: unless-stopped
